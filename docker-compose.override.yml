services:
  edge-planet:
    environment:
      RTSP_URL: ${RTSP_URL:-}
      EDGE_DEVICE_ID: ${EDGE_DEVICE_ID:-}
      REAL_INFERENCE_URL: ${REAL_INFERENCE_URL:-}
      CAMERA_POLL_INTERVAL_SECONDS: ${CAMERA_POLL_INTERVAL_SECONDS:-1.0}
      CAMERA_MIN_CONFIDENCE: ${CAMERA_MIN_CONFIDENCE:-0.75}
